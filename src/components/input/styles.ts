import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: '100%',
        height: 52,
        borderRadius: 10,
        borderColor: "#121214",
        paddingHorizontal: 16,
        fontSize: 16,
        padding: 12
    },
});